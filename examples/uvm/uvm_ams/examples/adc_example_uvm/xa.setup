#!/bin/bash 
######!/bin/csh -f
if (! $?XA_HOME ) then
      # setenv XA_HOME  /fs/src/interfaces/XA/E-2010.12-SP2-ENG1
      # setenv XA_HOME  /fs/src/interfaces/XA/E-2010.03-SP2-ENG2
      # setenv XA_HOME  /fs/src/interfaces/XA/G-2012.06-SP1
      #setenv XA_HOME  /fs/src/interfaces/XA/K-2015.06-SP1
      setenv XA_HOME /global/apps/xa_2020.03-SP1

endif
set path = ($XA_HOME/bin $path)
setenv LM_LICENSE_FILE  ${LM_LICENSE_FILE}
